[project]
name = "llm-bridge-types"
version = "0.1.0"
description = "Canonical types for llm-bridge — auto-generated from Go source."
requires-python = ">=3.10"
license = "MIT"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["llm_bridge_types*"]
